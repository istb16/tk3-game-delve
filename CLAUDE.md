# CLAUDE.md

DELVE — ターン制ローグライク・ダンジョン探索ゲーム。
*Go deeper. Survive longer.*

## このプロジェクトの絶対制約

出荷物は **`dist/delve.html` の 1 ファイルだけ**。ここが他のプロジェクトと最も違う点。

- ランタイムの外部ライブラリを追加しない（`dependencies` は空のまま）
- 外部画像ファイルを使わない。ドット絵は 16×16 のテキスト定義から
  SVG の `<symbol>` + `<use>` に変換して描く
- 外部音声ファイルを使わない。効果音は Web Audio API で**生成**する。
  既定は OFF。`AudioContext` は最初に鳴らす時まで作らない
  （起動時に作ると自動再生ポリシーで suspended のまま残る）
- 外部フォントを読み込まない。システムフォントスタックのみ
- ネットワークアクセスなしで完全に動作する

依存を足したくなったら、まずそれが `devDependencies`（ビルド時のみ）で済むか考える。
済まないなら実装方針を変える。

## コマンド

```bash
npm install        # 初回のみ
npm run dev        # 開発サーバ (http://localhost:5173)
npm run typecheck  # 型検査のみ
npm run build      # tsc → vite build → 単一HTML化 → dist/delve.html
```

`npm run build` の後は `dist/delve.html` をブラウザで直接開いて（`file://` で）動作確認する。
単一ファイル化の破綻はここでしか見つからない。

## アーキテクチャ

詳細は [docs/02-architecture.md](docs/02-architecture.md)。要点だけ:

```
ui/ audio/     ← 描画・入力・音（DOM を触るのはここだけ）
   ↕
game/          ← ゲームルール（DOM を一切知らない）
   ↓
core/ data/ storage/
```

**依存の向きは常に上から下。** これを守る限りロジックだけを単独で検証できる。

### 破ってはいけない境界

1. **`src/game/` と `src/core/` に `document` / `window` を書かない。**
   DOM が必要になったのなら、それは `ui/` の仕事。
2. **`ui/` からゲーム状態を書き換えない。**
   UI は状態を読んで描くだけ。入力は Intent に変換して `game/` に渡す。
3. **`Math.random()` を直接呼ばない。** `src/core/rng.ts` の seeded RNG を通す。
   同じシードで同じダンジョンが再現できることがデバッグの生命線。
4. **バランス数値をロジックに直接書かない。** `src/data/` のテーブルに置く。
   敵・装備・アイテム・パークの追加が「テーブルに 1 行足すだけ」で済む形を維持する。
5. **アニメーション状態を `ui/` に持たない。** 歩行フレームは `actor.steps % 2`、
   被弾演出は `actor.hurtOnTurn === state.turn` から導出する。
   `render()` が毎ターン innerHTML を作り直すので、クラスを付けるだけで
   CSS アニメーションは先頭から再生される。UI に可変状態を置くと
   この境界が形骸化する。
   ただし**見た目の選択**（サイドパネルのタブ）は `Settings` に置く —
   一時的な値を UI に抱えるより素直で、再描画で消えない。
6. **プレイヤーの派生値を書き換えるのは `recalcStats` だけ。** 累積加算せず、
   レベル・装備・パークから毎回ゼロで組み立て直す。装備を外したときの
   引き忘れが構造的に起きなくなる。
7. **敵の HP を直接減らさない。** 攻撃・爆弾・反射・継続ダメージはすべて `combat.damageEnemy` を通す。
   直接引くと撃破時の経験値とゴールドが入らない。
   **命中ログは `damageEnemy` を呼ぶ前に書く。** 後に回すと撃破・レベルアップのログが
   先に積まれ、「倒した → 殴った」という因果の逆転した並びになる。
8. **敵の手番はプレイヤーが動く前に確定させる。** 行動中に生まれた敵
   （Slime の分裂、イベントの番人）にその場で手番を与えると、
   分裂や報酬の受け取りが実質「無料の攻撃」になる。
9. **`game/` から音を呼ばない。** 1ターンぶんのログのキーを `ui/cues.ts` が
   音に引き当てる。新しい出来事にログを足せば音も付く。
10. **装備の優劣を単一のスコアで決めない。** スロットごとに1位が確定し、
   それを拾った時点で残りの装備が永久に使われなくなる。`compareEquipment` の
   支配関係で見て、トレードオフはプレイヤーに選ばせる。

## ターンの進み方

入力駆動。プレイヤーが入力するまで状態は 1 ミリも進まない（常時ループを回さない）。

```
入力 → Intent → resolvePlayerTurn() → resolveEnemyTurns() → 死亡判定 → render()
```

**壁への移動・無効な入力・UI 操作はターンを消費しない。**
「壁にぶつかった隙に殴られた」という理不尽を作らないため。

## 難易度を触るとき

「階を進むごとに難しくする」は感覚で決めない。指標は次の1本に集約されている。

```
Margin = (TTD / TTK) / k²     TTK: 敵を倒すターン数 / TTD: 殺されるまでのターン数 / k: 同時に相手取る敵数
```

**TTK はどの深度でも 1.5〜3 ターンに保つ。** 難易度は TTD を縮めることと k を増やすことで作る。
敵の HP を増やして戦闘を長くするのは、難しくしているのではなく退屈にしているだけ。

過去に踏んだ罠を2つ:

- **Margin だけを見ると足りない。** Margin は1回の交戦の余裕しか測らない。
  Run 全体の HP 収支（回復資源 × 供給量 vs フロアあたりの被ダメージ）が破綻していると、
  Margin が正しくても浅い階で終わる（docs/07 §7.8）。
- **敵の攻撃力はプレイヤーの防御力を確実に上回り続けなければならない。**
  減算式ダメージなので、防具とパークで防御力が追い越すと被ダメージが下限 1 に張り付き、
  プレイヤーが事実上不死になる。
- **出現の重み付けは「出てよいか」を決められない。** ガウス重みの裾は浅い階まで伸びる。
  `minFloor` の下限判定を別に持たないと 1 階に Warden が湧く。
- **ボスの素の値は「その階で成立する」大きさに置く。** 素で強い数値にすると
  階層係数が乗った瞬間に手も足も出なくなる。特に防御を低く保つこと。
- **倍率は1箇所にだけ持たせる。** ステータス効果に倍率を持たせたうえで
  `attack` も書き換えると、掛け算が二重になる（実測でボスの激昂が
  1.5倍のつもりで2.25倍になっていた）。
- **係数を合わせる前に、数値が仕様どおり動いているか確かめる。**
  バグがあるまま調整すると、係数がそのバグを吸収してしまう。

バランス数値を変えたら**必ず**:

```bash
node tools/difficulty-model.mjs
```

目標比が 0.7〜1.5 を外れた階層があれば原因を特定し、[docs/07](docs/07-difficulty.md) の表を更新する。

## ドット絵を触るとき

- 1 スプライト = 16×16、正面向きのみ、歩行は **2 フレーム**。
- フレーム間の差分は**足元だけ 1〜2 ピクセル**。大きく動かすと痙攣して見える。
- 輪郭は必ず `#101018` で囲む。当時のドット絵の識別性はこの1ピクセルの黒縁に依存している。
- 描画は `<defs>` の `<symbol>` に一度だけ定義し `<use>` で参照する。
  1ピクセル1 `<rect>` で素直に描くと 57,600 要素になり破綻する。
- ルート `<svg>` に `shape-rendering="crispEdges"` を必ず付ける。ないとドットが滲む。

## 実装の順序

[docs/09-roadmap.md](docs/09-roadmap.md) のフェーズに従う。原則:

- **基本ループ（移動 → 敵 → 戦闘 → 階段 → 次の階）を最優先で完成させる。**
- 各フェーズの終わりで必ず遊べる状態にしてコミットする。壊れたまま次へ進まない。
- **演出は最後。** ゲームプレイが面白くなる前に見た目を磨かない。

## コード規約

| 対象 | 規約 |
|---|---|
| ファイル名 | kebab-case・単数形 (`enemy.ts`) |
| 型 | PascalCase (`GameState`) |
| 関数・変数 | camelCase (`resolvePlayerTurn`) |
| 定数 | UPPER_SNAKE_CASE (`GRID_SIZE`) |
| データテーブル | 複数形 (`ENEMIES`, `PERKS`) |
| CSS クラス | kebab-case + BEM 風 (`.hud__stat`, `.tile--wall`) |

- TypeScript は `strict` + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`。
  配列アクセスは `undefined` を返す前提で書く。
- 型のインポートは `import type { ... }`（`verbatimModuleSyntax` のため必須）。
- コメントは「なぜそうしたか」を書く。「何をしているか」はコードで示す。

## ドキュメント

`docs/` が設計の正本。実装が設計とずれたら、**コードを直すか docs を直すか**を選ぶ。
放置してどちらが正しいか分からない状態にしない。

| ドキュメント | 内容 |
|---|---|
| [01-requirements.md](docs/01-requirements.md) | 要件・完成条件 |
| [02-architecture.md](docs/02-architecture.md) | レイヤ構成・ターンループ・ビルド |
| [03-game-design.md](docs/03-game-design.md) | 敵・装備・アイテム・パークの定義 |
| [04-data-model.md](docs/04-data-model.md) | 型定義・セーブデータ・実装状況 |
| [05-ui-design.md](docs/05-ui-design.md) | ドット絵仕様・レイアウト・favicon・入力 |
| [06-dungeon-generation.md](docs/06-dungeon-generation.md) | 生成アルゴリズム |
| [07-difficulty.md](docs/07-difficulty.md) | 難易度カーブと5つのレバー |
| [08-verification.md](docs/08-verification.md) | 不変条件・テスト・単一ファイル検証 |
| [09-roadmap.md](docs/09-roadmap.md) | フェーズと受け入れ基準 |

## 設計判断で迷ったときの基準

このゲームの面白さは以下の掛け合わせから来る。

```
ランダム生成 × キャラクター成長 × 装備 × ビルド × リスク判断
```

機能を足すか迷ったら「**プレイヤーに新しい判断を生むか**」で決める。
生まないなら、それはただの複雑さ。

ノーリスクの得を作らない。すべての報酬には対価か賭けを置く。

## コミット

日本語で書く。1 コミット = 1 つのまとまった変更。壊れた状態をコミットしない。
